# Placeholder for application logic


def sample_function(value: int) -> int:
    """
    A sample function to be tested.
    """
    return value * 2


def process_markdown_block(markdown_content: str) -> str:
    """
    Placeholder for processing a single markdown block.
    This might involve calling Pandoc.
    """
    # In a real scenario, this would interact with Pandoc
    # For now, just a simple transformation
    return f"Processed: {markdown_content.strip()}"
